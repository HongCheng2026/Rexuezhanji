# GPT Web Project Brief

## 当前项目

我们正在制作一个小游戏项目，项目名称可以暂定为“20260618-游戏”。

当前核心交付目录是：

`E:\JT\20260618-游戏`

后续所有和这个游戏相关的文件，默认都应该放在这个目录下。

## 我们正在做什么

我们正在把这个项目逐步做成一个可以直接运行、可以试玩、可以继续迭代的网页小游戏。

优先目标不是只写概念，而是每次都尽量产出实际可用的内容，例如：

- 可打开运行的游戏页面
- 游戏逻辑代码
- 样式和界面优化
- 角色、飞机、敌人、背景、按钮、图标等素材
- 生成素材用的提示词
- 游戏说明文档
- 截图、导出文件、打包文件

## 默认技术方向

除非用户明确要求换技术方案，否则默认使用：

- HTML
- CSS
- JavaScript

主要文件优先保持为：

- `index.html`
- `style.css`
- `game.js`

每次修改后，应尽量保持 `index.html` 可以直接打开并运行游戏。

## 交付目录规则

所有产出文件默认保存到：

`E:\JT\20260618-游戏`

包括但不限于：

- 源代码文件
- 图片素材
- 音效素材
- 提示词文档
- 游戏设计文档
- 测试截图
- 导出版本
- 打包文件

除非用户明确指定其他位置，否则不要把交付文件放到别的目录。

## 游戏制作要求

每次迭代都应尽量让游戏更接近“可玩成品”，而不是只停留在想法阶段。

优先补全这些内容：

- 清晰的开始、进行中、结束、重开流程
- 玩家操作反馈
- 分数、生命值、关卡、倒计时等状态展示
- 胜利或失败条件
- 移动端和电脑端都能正常显示
- 按钮、文字、游戏元素不要重叠
- 界面尽量清楚、顺眼、有游戏感

## 素材与视觉方向

如果需要生成游戏素材，生成后的文件也应保存在：

`E:\JT\20260618-游戏`

素材可以包括：

- 玩家飞机
- 敌机
- 子弹
- 爆炸效果
- 背景
- UI 按钮
- 图标
- 封面图

如果需要写素材生成提示词，也把提示词保存成 Markdown 文件放在同一目录。

## 回复和协作偏好

默认使用中文沟通。

说明要实用、直接，优先告诉用户：

- 做了什么
- 文件在哪里
- 怎么运行或预览
- 下一步可以继续做什么

如果有不确定的地方，优先做出合理选择，让项目继续向可试玩版本推进。

## 给 GPT 的关键提醒

这是一个正在进行中的小游戏项目。请始终围绕“把游戏做成可运行、可试玩、可继续迭代的版本”来工作。

所有交付物默认放在：

`E:\JT\20260618-游戏`

不要只给抽象建议，尽量产出可以直接使用的文件、代码、素材提示词或改进方案。
