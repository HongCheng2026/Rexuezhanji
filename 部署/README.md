# 热血战机部署目录

这里把同一个小游戏拆成四块：

- `通用代码`：以后优先修改这里的可复用文件。
- `Netlify-H5`：H5 网页版，当前优先部署这个目录。
- `微信小程序`：微信开发者工具导入目录。
- `抖音小程序`：抖音开发者工具导入目录，已按 `.ttml/.ttss` 结构准备。

推荐流程：

1. 修改 `通用代码`。
2. 运行 `同步通用代码.ps1`。
3. H5 上传 `Netlify-H5` 文件夹或 `Netlify-H5.zip`。
4. 小程序分别用对应开发者工具打开目录继续适配、预览和上传。

