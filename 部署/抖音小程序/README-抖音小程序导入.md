# 抖音小程序导入说明

这个目录是从微信小程序目录转换出来的抖音小程序骨架：

- 页面模板已从 `.wxml` 转为 `.ttml`。
- 样式已从 `.wxss` 转为 `.ttss`。
- JS 里的 `wx.` API 前缀已替换为 `tt.`。

后续用抖音开发者工具打开此目录，检查画布、资源路径、登录与上传配置即可。

当前优先上线的是 Netlify H5 版，小程序目录先作为后续部署基础。

